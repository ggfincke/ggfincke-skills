---
name: skill-name
description: Describe what this skill does and include concrete trigger phrases or task contexts for when agents should use it.
---

# Skill Name

## Workflow

1. Inspect the local context needed for the task.
2. Follow the project conventions before adding new patterns.
3. Make the smallest change that satisfies the request.
4. Run the relevant validation command and report any failures.

## Resources

- Use `references/` for detailed background that should be loaded only when needed.
- Use `scripts/` for repeatable commands or fragile workflows.
- Use `assets/` for reusable files that become part of an output.

